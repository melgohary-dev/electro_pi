/** Number of products to display per page in the infinite scroll. */
export const PRODUCTS_PER_PAGE = 8;

/** Base URL for the Fake Store API. */
export const API_BASE_URL = "https://fakestoreapi.com";

/** Default stale time for React Query (5 minutes). */
export const STALE_TIME = 5 * 60 * 1000;

/** Default garbage collection time for React Query (10 minutes). */
export const CACHE_TIME = 10 * 60 * 1000;
